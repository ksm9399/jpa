# 디비 연결
# 테스트 설정 파일에서는 디비 관련 연결 설정을 지워도 자동으로 인메모리 설정으로 맞춰서 테스트 실행
# spring:
#   datasource:
#     url: jdbc:h2:mem:test
#     username: sa
#     password:
#     driver-class-name: org.h2.Driver

#   # jpa
#   jpa:
#     hibernate:
#       ddl-auto: create-drop
#     properties:
#       hibernate:
#         '[show_sql]': true
#         '[format_sql]': true

logging:
  level:
    '[org.hibernate.sql]': debug
    '[org.hibernate.orm.jdbc.bind]': trace